import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  searchContainer: {
    marginVertical: 16,
  },
  searchInput: {
    height: 40,
    paddingHorizontal: 8,
    borderWidth: 1,
    borderRadius: 4,
    borderColor: '#ccc',
    backgroundColor: '#fff',
    color: '#333',
  },
});

export default styles;
