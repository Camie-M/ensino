import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  baseLayoutContainer: {
    flex: 1,
    backgroundColor: '#fff',
  },
  contentContainer: {
    padding: 16,
  },
});

export default styles;
